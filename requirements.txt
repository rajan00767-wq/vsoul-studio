# Vsoul Studio local web app: Qwen Image Edit + Qwen-VL only.
# Install the CUDA-compatible PyTorch build first, then this file.

# Web API and local UI bridge
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
gradio>=4.44.0
psutil>=6.0.0

# CUDA runtime
torch>=2.3.0
torchvision>=0.18.0

# Image processing and identity validation
opencv-python>=4.10.0,<5.0
Pillow>=11.0.0
numpy>=2.0.0
insightface>=0.7.3
onnxruntime-gpu>=1.19.0; sys_platform == "win32" or sys_platform == "linux"
onnxruntime>=1.19.0; sys_platform == "darwin"

# Qwen Image Edit 2511 and Qwen2.5-VL analysis
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.34.0
safetensors>=0.4.5
huggingface_hub>=0.25.0
einops>=0.8.0
PyYAML>=6.0
bitsandbytes>=0.44.0
gguf>=0.10.0

# Optional local 2x/4x export path
realesrgan>=0.3.0
basicsr>=1.4.2

tqdm>=4.66.4
